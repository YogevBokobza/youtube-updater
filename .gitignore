*.iml
.gradle/
.idea/
.DS_Store
build/
captures/
.externalNativeBuild
.cxx
local.properties

# Signing — never commit keystores or secrets
*.jks
*.keystore
keystore.properties

# Built APKs (distributed via GitHub Releases, not committed)
*.apk
